// Haptic feedback utility (disabled for React Native 0.81 compatibility)
export const triggerHaptic = (type: 'light' | 'medium' | 'heavy' | 'success' | 'error' = 'medium') => {
  // Haptic feedback disabled for React Native 0.81 compatibility
  console.log('Haptic feedback (disabled):', type);
};
